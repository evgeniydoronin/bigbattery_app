# 📱 SETTINGS SCREEN - Детальная логика работы

**Дата создания:** 06.10.2025
**Версия:** 1.0
**Автор:** Технический анализ кодовой базы

---

## 📋 СОДЕРЖАНИЕ

1. [Обзор архитектуры](#обзор-архитектуры)
2. [Module ID Logic - Ядро системы](#module-id-logic---ядро-системы)
3. [Цепочка вызовов](#цепочка-вызовов)
4. [Детальный анализ кода](#детальный-анализ-кода)
5. [Визуальные схемы](#визуальные-схемы)
6. [FAQ и примеры](#faq-и-примеры)
7. [Связанные файлы](#связанные-файлы)

---

## 🏗️ ОБЗОР АРХИТЕКТУРЫ

### Назначение Settings экрана

Settings экран отвечает за конфигурацию протоколов связи BMS (Battery Management System):
- **Module ID** - идентификатор батареи в системе (1-16)
- **CAN Protocol** - протокол связи CAN для инверторов
- **RS485 Protocol** - протокол связи RS485 для мониторинга

### Ключевые компоненты

```
SettingsViewController
    ├── headerLogoView (HeaderLogoView)
    ├── connectionStatusBanner (ConnectionStatusBanner)
    ├── protocolSettingsHeader (UILabel)
    ├── moduleIdSettingItemView (SettingItemView?)
    ├── canProtocolView (SettingItemView?)
    ├── rs485ProtocolView (SettingItemView?)
    ├── saveButton (UIButton)
    └── informationBanner (UIView)
```

### Зависимости

**Внутренние:**
- `Zetara.Data.ModuleIdControlData` - данные Module ID
- `Zetara.Data.CANControlData` - данные CAN протокола
- `Zetara.Data.RS485ControlData` - данные RS485 протокола
- `ZetaraManager` - менеджер Bluetooth коммуникации
- `SettingItemView` - переиспользуемый компонент селекта

**Внешние:**
- `RxSwift` - реактивное программирование
- `RxBluetoothKit2` - Bluetooth LE коммуникация

---

## 🔑 MODULE ID LOGIC - Ядро системы

### Критическое правило

**📌 ПРОТОКОЛЫ CAN И RS485 ДОСТУПНЫ ТОЛЬКО ДЛЯ MODULE ID = 1**

**Код:**
```swift
// Файл: Zetara/Sources/ControlData.swift:99-101
public func otherProtocolsEnabled() -> Bool {
    moduleId == 1
}
```

### Почему так сделано?

#### Контекст BMS архитектуры

BMS может работать в двух режимах:

**1. РЕЖИМ "ID 1" (Master Battery - Мастер-батарея):**
```
✅ Роль: Главная батарея в системе
✅ Функции:
   - Общается с внешними устройствами (инвертор, солнечные панели)
   - Отправляет данные через CAN/RS485
   - Координирует работу подчиненных батарей
✅ Протоколы: CAN и RS485 АКТИВНЫ
```

**2. РЕЖИМ "ID 2-16" (Slave Batteries - Подчиненные батареи):**
```
❌ Роль: Дополнительная батарея в системе
❌ Функции:
   - НЕ общается с внешними устройствами
   - Слушает команды от мастер-батареи (ID 1)
   - Синхронизируется с мастером
❌ Протоколы: CAN и RS485 НЕАКТИВНЫ
```

#### Архитектура параллельного подключения

```
          ┌──────────────┐
          │  Инвертор    │
          └──────┬───────┘
                 │ CAN/RS485
                 ↓
          ┌──────────────┐
          │ Батарея ID 1 │ ← МАСТЕР (протоколы активны)
          │   (Master)   │
          └──────┬───────┘
                 │ Внутренняя шина
       ┌─────────┼─────────┐
       ↓         ↓         ↓
   ┌───────┐ ┌───────┐ ┌───────┐
   │ID 2   │ │ID 3   │ │ID 4   │ ← ПОДЧИНЕННЫЕ (протоколы неактивны)
   │(Slave)│ │(Slave)│ │(Slave)│
   └───────┘ └───────┘ └───────┘
```

### Поддерживаемые значения Module ID

**Диапазон:** 1-16

**Код:**
```swift
// Файл: Zetara/Sources/ControlData.swift:72
static let supportedIds = Array(1...16)
```

**Отображение в UI:**
```
ID 1  → "ID 1"  → Протоколы АКТИВНЫ   ✅
ID 2  → "ID 2"  → Протоколы НЕАКТИВНЫ ❌
ID 3  → "ID 3"  → Протоколы НЕАКТИВНЫ ❌
...
ID 16 → "ID 16" → Протоколы НЕАКТИВНЫ ❌
```

---

## 🔄 ЦЕПОЧКА ВЫЗОВОВ

### Lifecycle диаграмма

```
┌─────────────────────────────────────────────────────────────┐
│                     viewDidLoad()                            │
│                                                              │
│  1. Создание UI компонентов                                 │
│  2. toggleRS485AndCAN(false) ← ПРОТОКОЛЫ ВЫКЛЮЧЕНЫ         │
│  3. Подписка на события selectedOptionIndex                │
│                                                              │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                   viewWillAppear()                           │
│                                                              │
│  Скрываем navigation bar                                    │
│                                                              │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│        rx.isVisible (когда экран становится видимым)        │
│                                                              │
│  if deviceConnected && protocolDataIsEmpty:                 │
│     → getAllSettings()                                      │
│                                                              │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                   getAllSettings()                           │
│                                                              │
│  1. queuedRequest { getModuleId() }                         │
│     ↓                                                        │
│  2. Получен ModuleIdControlData                             │
│     ↓                                                        │
│  3. toggleRS485AndCAN(idData.otherProtocolsEnabled())       │
│     ↓                                                        │
│  4. if moduleId == 1 → ПРОТОКОЛЫ АКТИВНЫ ✅                │
│     if moduleId != 1 → ПРОТОКОЛЫ НЕАКТИВНЫ ❌              │
│     ↓                                                        │
│  5. queuedRequest { getRS485() }                            │
│     ↓                                                        │
│  6. queuedRequest { getCAN() }                              │
│                                                              │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│   ПОЛЬЗОВАТЕЛЬ ВЫБИРАЕТ НОВЫЙ MODULE ID                     │
│                                                              │
│  selectedOptionIndex.onNext(index)                          │
│     ↓                                                        │
│  setModuleId(at: index)                                     │
│     ↓                                                        │
│  toggleRS485AndCAN(index == 0)                              │
│     ↓                                                        │
│  if index == 0 (ID 1) → ПРОТОКОЛЫ АКТИВНЫ ✅               │
│  if index != 0 (ID 2-16) → ПРОТОКОЛЫ НЕАКТИВНЫ ❌          │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 📝 ДЕТАЛЬНЫЙ АНАЛИЗ КОДА

### 1. Инициализация (viewDidLoad)

**Файл:** `SettingsViewController.swift:265-321`

**Функция:**
```swift
override func viewDidLoad() {
    super.viewDidLoad()

    // ...создание UI...

    // КРИТИЧНО: Протоколы выключены по умолчанию
    self.toggleRS485AndCAN(false)  // ← Строка 320
}
```

**Что происходит:**
1. Создаются UI компоненты (селекты, кнопки)
2. Протоколы **выключены** как безопасное начальное состояние
3. Подписки на изменения (selectedOptionIndex)

**Зачем:**
- Предотвращает случайные клики до загрузки данных
- Безопасное начальное состояние

---

### 2. Загрузка настроек (getAllSettings)

**Файл:** `SettingsViewController.swift:717-774`

**Функция:**
```swift
func getAllSettings() {
    Alert.show("Loading...", timeout: 3)

    // ШАГ 1: Загрузка Module ID через Request Queue
    ZetaraManager.shared.queuedRequest {
        ZetaraManager.shared.getModuleId()
    }
    .subscribe(onSuccess: { [weak self] idData in
        self?.moduleIdData = idData
        ZetaraManager.shared.cachedModuleIdData = idData

        // КРИТИЧЕСКАЯ ЛОГИКА! ⬇️
        // Проверяем: moduleId == 1?
        self?.toggleRS485AndCAN(idData.otherProtocolsEnabled())
        // ⬆️ Если true → протоколы активны
        // ⬆️ Если false → протоколы неактивны

        // Загружаем протоколы
        self?.getRS485().subscribe(...)
        self?.getCAN().subscribe(...)
    })
}
```

**Параметры:**
- Нет входных параметров

**Возвращает:**
- Void (асинхронная операция через RxSwift)

**Side effects:**
- Обновляет `moduleIdData`, `rs485Data`, `canData`
- Активирует/деактивирует протоколы
- Обновляет UI (labels в селектах)

**Ошибки:**
- Timeout после 10 секунд (Request Queue)
- Обрабатываются через `onError` блок

---

### 3. Переключение протоколов (toggleRS485AndCAN)

**Файл:** `SettingsViewController.swift:712-715`

**Функция:**
```swift
func toggleRS485AndCAN(_ enabled: Bool) {
    self.rs485ProtocolView?.optionsButton.isEnabled = enabled
    self.canProtocolView?.optionsButton.isEnabled = enabled
}
```

**Параметры:**
- `enabled: Bool` - true = активны, false = неактивны

**Что делает:**
- Устанавливает `isEnabled` для кнопок селектов
- `true` → кнопки кликабельные, черные
- `false` → кнопки некликабельные, серые

**Вызывается из:**
1. `viewDidLoad()` - начальное состояние (false)
2. `getAllSettings()` - после загрузки данных
3. `setModuleId()` - после изменения Module ID

---

### 4. Изменение Module ID (setModuleId)

**Файл:** `SettingsViewController.swift:782-799`

**Функция:**
```swift
func setModuleId(at index: Int, completion: (() -> Void)? = nil) {
    // index = 0 → ID 1
    // index = 1 → ID 2
    // ...
    // index = 15 → ID 16

    // Отправка команды BMS
    ZetaraManager.shared.setModuleId(index + 1)
        .subscribe { [weak self] success in
            if success, let idData = self?.moduleIdData {
                // Обновляем label
                self?.moduleIdSettingItemView?.label = idData.readableId(at: index)

                // КРИТИЧНО! ⬇️
                self?.toggleRS485AndCAN(index == 0)
                // ⬆️ index == 0 (ID 1) → протоколы АКТИВНЫ
                // ⬆️ index != 0 (ID 2-16) → протоколы НЕАКТИВНЫ
            }
            completion?()
        }
}
```

**Параметры:**
- `index: Int` - индекс в массиве (0-15)
- `completion: (() -> Void)?` - callback после завершения

**Логика индексов:**
```
index 0  → moduleId 1  → toggleRS485AndCAN(true)  ✅
index 1  → moduleId 2  → toggleRS485AndCAN(false) ❌
index 2  → moduleId 3  → toggleRS485AndCAN(false) ❌
...
index 15 → moduleId 16 → toggleRS485AndCAN(false) ❌
```

**Timeout:** 3 секунды

---

### 5. Проверка доступности (otherProtocolsEnabled)

**Файл:** `Zetara/Sources/ControlData.swift:99-101`

**Функция:**
```swift
public func otherProtocolsEnabled() -> Bool {
    moduleId == 1
}
```

**Тип:** `ModuleIdControlData` extension

**Параметры:**
- Нет (использует `self.moduleId`)

**Возвращает:**
- `true` если `moduleId == 1`
- `false` если `moduleId != 1`

**Простая логика:**
```swift
moduleId = 1  → true  → Протоколы АКТИВНЫ
moduleId = 2  → false → Протоколы НЕАКТИВНЫ
moduleId = 3  → false → Протоколы НЕАКТИВНЫ
...
```

**Это ЯДРО всей системы!**

---

## 📊 ВИЗУАЛЬНЫЕ СХЕМЫ

### State Machine (Машина состояний)

```
                    ┌──────────────────┐
                    │   APP START      │
                    └────────┬─────────┘
                             ↓
                    ┌──────────────────┐
                    │  viewDidLoad()   │
                    │                  │
                    │ Protocols: OFF   │
                    └────────┬─────────┘
                             ↓
                    ┌──────────────────┐
                    │ getAllSettings() │
                    │                  │
                    │ Loading...       │
                    └────────┬─────────┘
                             ↓
                  ┌──────────┴──────────┐
                  │                     │
          ┌───────▼────────┐    ┌──────▼────────┐
          │ moduleId == 1? │    │ moduleId != 1?│
          │                │    │                │
          │ Protocols: ON  │    │ Protocols: OFF │
          └───────┬────────┘    └──────┬────────┘
                  │                     │
                  │                     │
          ┌───────▼──────────────────────▼────────┐
          │     USER SELECTS NEW MODULE ID        │
          └───────┬──────────────────────┬────────┘
                  │                      │
          ┌───────▼────────┐    ┌───────▼────────┐
          │  index == 0?   │    │  index != 0?   │
          │   (ID 1)       │    │  (ID 2-16)     │
          │                │    │                │
          │ Protocols: ON  │    │ Protocols: OFF │
          └────────────────┘    └────────────────┘
```

### Dataflow (Поток данных)

```
┌─────────────┐
│    BMS      │ Bluetooth
│  (Battery)  │◄─────────────┐
└──────┬──────┘              │
       │                     │
       │ Response            │ Request
       ↓                     │
┌──────────────────┐         │
│ ZetaraManager    │─────────┘
│ .getModuleId()   │
└────────┬─────────┘
         │
         │ ModuleIdControlData
         ↓
┌──────────────────────────┐
│ SettingsViewController   │
│ .getAllSettings()        │
└────────┬─────────────────┘
         │
         │ .otherProtocolsEnabled()
         ↓
┌──────────────────────────┐
│ if moduleId == 1:        │
│   → true                 │
│ else:                    │
│   → false                │
└────────┬─────────────────┘
         │
         │ Bool (enabled)
         ↓
┌──────────────────────────┐
│ toggleRS485AndCAN()      │
│                          │
│ rs485.isEnabled = Bool   │
│ can.isEnabled = Bool     │
└──────────────────────────┘
         │
         ↓
┌──────────────────────────┐
│        UI UPDATE         │
│                          │
│ Buttons: Active/Inactive │
└──────────────────────────┘
```

### UI State Comparison

```
┌──────────────────────────────────────────────────────────┐
│                      MODULE ID = 1                        │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  📦 Module ID:  [ID 1 ▼] ← АКТИВЕН                      │
│                                                           │
│  🔌 CAN Protocol:  [PYLON ▼] ← АКТИВЕН (черный)         │
│                                                           │
│  🔌 RS485 Protocol: [Generic ▼] ← АКТИВЕН (черный)      │
│                                                           │
│  [Save] ← АКТИВЕН при изменениях                         │
│                                                           │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                      MODULE ID = 2                        │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  📦 Module ID:  [ID 2 ▼] ← АКТИВЕН                      │
│                                                           │
│  🔌 CAN Protocol:  [-- ▼] ← НЕАКТИВЕН (серый) ❌        │
│                                                           │
│  🔌 RS485 Protocol: [-- ▼] ← НЕАКТИВЕН (серый) ❌       │
│                                                           │
│  [Save] ← НЕАКТИВЕН (серый)                              │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

---

## ❓ FAQ И ПРИМЕРЫ

### Вопрос 1: Почему протоколы неактивны?

**Ответ:**
Потому что текущий Module ID ≠ 1. Протоколы доступны **ТОЛЬКО** для ID 1.

**Решение:**
Выберите "ID 1" в селекте Module ID → Протоколы активируются автоматически.

---

### Вопрос 2: Что если у меня несколько батарей?

**Ответ:**
При параллельном подключении:
- **Одна батарея** должна иметь ID 1 (мастер)
- **Остальные батареи** должны иметь ID 2-16 (подчиненные)

**Пример:**
```
Батарея 1: ID 1 → Общается с инвертором через CAN
Батарея 2: ID 2 → Слушает команды от батареи 1
Батарея 3: ID 3 → Слушает команды от батареи 1
```

---

### Вопрос 3: Можно ли изменить логику и разрешить протоколы для всех ID?

**Технически:** Да, можно изменить код.

**Но НЕ РЕКОМЕНДУЕТСЯ:**
- Это нарушит архитектуру BMS
- Может вызвать конфликты при параллельном подключении
- Инвертор будет получать данные от нескольких батарей одновременно

**Как изменить (НЕ ДЕЛАЙТЕ!):**
```swift
// Файл: Zetara/Sources/ControlData.swift:99-101
public func otherProtocolsEnabled() -> Bool {
    return true  // ← Всегда true (ОПАСНО!)
}
```

---

### Вопрос 4: Как протестировать смену Module ID?

**Шаги:**
1. Подключитесь к батарее
2. Откройте Settings
3. Посмотрите текущий Module ID
4. Выберите другой ID (например, "ID 1")
5. Наблюдайте, как протоколы активируются
6. Выберите "ID 2"
7. Наблюдайте, как протоколы деактивируются
8. **НЕ НАЖИМАЙТЕ Save** без необходимости!

---

### Вопрос 5: Что происходит при сохранении?

**Код:** `SettingsViewController.swift:643-688`

**Шаги:**
1. Пользователь нажимает "Save"
2. Приложение отправляет 3 команды BMS:
   - `setModuleId()`
   - `setRS485()`
   - `setCAN()`
3. BMS сохраняет настройки в энергонезависимую память
4. Показывается popup: "Restart the battery..."
5. **ВАЖНО:** Батарею НУЖНО перезапустить кнопкой питания!
6. Переподключиться к приложению
7. Проверить, что настройки применились

---

## 📁 СВЯЗАННЫЕ ФАЙЛЫ

### Основные файлы

| Файл | Строки | Описание |
|------|--------|----------|
| **SettingsViewController.swift** | 863 | Главный контроллер Settings экрана |
| ├─ viewDidLoad | 265-321 | Инициализация, toggleRS485AndCAN(false) |
| ├─ getAllSettings | 717-774 | Загрузка настроек через Request Queue |
| ├─ toggleRS485AndCAN | 712-715 | Активация/деактивация протоколов |
| ├─ setModuleId | 782-799 | Изменение Module ID |
| ├─ setRS485 | 801-817 | Изменение RS485 протокола |
| ├─ setCAN | 819-835 | Изменение CAN протокола |
| └─ saveTapped | 643-688 | Сохранение всех настроек |

| Файл | Строки | Описание |
|------|--------|----------|
| **Zetara/Sources/ControlData.swift** | 200+ | Структуры данных протоколов |
| ├─ ModuleIdControlData | 69-102 | Данные Module ID |
| │  ├─ supportedIds | 72 | Array(1...16) |
| │  ├─ readableIds() | 87-89 | ["ID 1", ..., "ID 16"] |
| │  ├─ readableId() | 91-93 | "ID X" текущий |
| │  └─ otherProtocolsEnabled() | 99-101 | **ЯДРО ЛОГИКИ!** |
| ├─ RS485ControlData | 104-154 | Данные RS485 |
| └─ CANControlData | 156-205 | Данные CAN |

| Файл | Строки | Описание |
|------|--------|----------|
| **ZetaraManager.swift** | 642 | Менеджер Bluetooth коммуникации |
| ├─ getModuleId() | ~540 | Запрос текущего Module ID |
| ├─ setModuleId() | ~550 | Установка нового Module ID |
| ├─ getRS485() | ~560 | Запрос RS485 настроек |
| ├─ setRS485() | ~570 | Установка RS485 протокола |
| ├─ getCAN() | ~580 | Запрос CAN настроек |
| ├─ setCAN() | ~590 | Установка CAN протокола |
| └─ queuedRequest() | 323-361 | Request Queue (500ms интервал) |

| Файл | Строки | Описание |
|------|--------|----------|
| **SettingItemView.swift** | ~200 | Переиспользуемый компонент селекта |
| ├─ optionsButton | ~50 | UIButton для открытия меню |
| ├─ expandOptionsMenu() | ~120 | Показ выпадающего меню |
| └─ selectedOptionIndex | ~80 | BehaviorSubject<Int> |

---

## 🔧 ТЕХНИЧЕСКАЯ СПРАВКА

### Request Queue механизм

**Назначение:** Предотвращение конфликтов при множественных Bluetooth запросах.

**Параметры:**
- Минимальный интервал: 500ms
- Timeout: 10 секунд
- Режим: Последовательный (очередь)

**Использование:**
```swift
ZetaraManager.shared.queuedRequest {
    ZetaraManager.shared.getModuleId()
}
.subscribe(onSuccess: { data in
    // Обработка данных
})
```

---

### RxSwift subscriptions

**Где используется:**
- `selectedOptionIndex` - отслеживание выбора пользователя
- `connectedPeripheralSubject` - статус подключения
- `.skip(1)` - игнорирование начального значения

**Пример:**
```swift
moduleIdSettingItemView?.selectedOptionIndex
    .skip(1)  // Пропускаем начальное значение
    .subscribe { [weak self] index in
        self?.pendingModuleIdIndex = index
        // Показываем status indicator
    }
```

---

### Edge Cases

**1. Нет подключения:**
- getAllSettings() не вызывается
- Протоколы остаются неактивными

**2. Timeout при загрузке:**
- Показывается ошибка
- Протоколы остаются в текущем состоянии

**3. Изменение ID во время загрузки:**
- Request Queue обеспечивает последовательность
- Не будет конфликтов

**4. Быстрое переключение между экранами:**
- viewWillDisappear() отменяет subscriptions
- Предотвращает memory leaks

---

## 📝 CHANGELOG

### Версия 1.0 (06.10.2025)
- Создана первая версия документации
- Добавлены все основные секции
- Детальный анализ кода с номерами строк
- Визуальные схемы и диаграммы
- FAQ и примеры использования

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

Для разработчиков:

1. **При добавлении нового протокола:**
   - Создать новую структуру в `ControlData.swift`
   - Добавить методы get/set в `ZetaraManager.swift`
   - Добавить SettingItemView в `SettingsViewController.swift`
   - Обновить `toggleRS485AndCAN()` если нужна условная логика

2. **При изменении логики доступности:**
   - Изменить `otherProtocolsEnabled()` в `ControlData.swift:99-101`
   - Обновить эту документацию!

3. **При рефакторинге:**
   - Сохранить основную логику (moduleId == 1)
   - Обновить номера строк в документации
   - Проверить все цепочки вызовов

---

**Конец документа**

Эта документация должна избавить от необходимости каждый раз анализировать код для понимания логики Settings экрана!
