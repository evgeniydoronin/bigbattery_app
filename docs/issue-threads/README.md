# Issue Threads - Система отслеживания проблем

## Что такое Issue Thread?

**Issue Thread** - это "нить" проблемы, которая отслеживает:
- Историю проблемы от первого появления до полного решения
- Все попытки исправления (successful и failed)
- Что улучшилось / что ухудшилось после каждого fix
- Контекст для будущих сессий

## Зачем это нужно?

### Проблема БЕЗ Thread System:
```
Получили логи от клиента → Анализируем как новую проблему →
Не помним что делали в прошлый раз →
Повторяем те же ошибки →
Не видим паттерны →
Каждая сессия начинается с нуля
```

### Решение С Thread System:
```
Получили логи → Читаем Thread →
Знаем всю историю проблемы →
Видим что уже пробовали →
Сравниваем с expected behavior →
Понимаем что улучшилось/ухудшилось →
Делаем INFORMED решение! →
Не повторяем ошибки
```

---

## Когда создавать новый Thread?

Создавай новый thread когда:
- ✅ Проблема повторяется после fix (regression)
- ✅ Клиент сообщает о НОВОЙ проблеме (не покрыто existing threads)
- ✅ Проблема эволюционирует (меняет симптомы, но root cause тот же)

НЕ создавай новый thread если:
- ❌ Это одноразовая проблема (user error, не bug)
- ❌ Уже есть активный thread для этой проблемы

---

## Структура Thread файла

См. шаблон: `THREAD-TEMPLATE.md`

**Обязательные секции:**
1. **CURRENT STATUS** - что происходит сейчас, next steps
2. **TIMELINE** - хронология всех attempts (oldest first!)
3. **ROOT CAUSE EVOLUTION** - как менялось понимание проблемы
4. **METRICS** - количественные данные (что улучшилось/ухудшилось)
5. **SUCCESS CRITERIA** - когда можно считать resolved
6. **LESSONS LEARNED** - выводы для future work

---

## Workflow: Как использовать Thread System

### 1️⃣ Получили новые логи от клиента

**ПЕРВЫМ ДЕЛОМ:**
```bash
1. Открой docs/issue-threads/README.md
2. Посмотри таблицу "Активные Threads"
3. Найди relevant thread (по симптомам)
```

### 2️⃣ Thread найден - Continuation

**Прочитай ПОЛНОСТЬЮ thread файл:**
- [ ] Секция TIMELINE - все предыдущие attempts
- [ ] Секция ROOT CAUSE EVOLUTION - текущее понимание
- [ ] Секция METRICS - что уже улучшилось/ухудшилось
- [ ] Последний attempt - что ожидали увидеть в логах

**Сравни новые логи с expected behavior:**

| Expected (from last fix) | Reality (from new logs) | Status |
|-------------------------|------------------------|---------|
| [X] должно быть в логах | [Y] реально в логах | ✅/❌ |

**Update thread:**
- Добавь новую запись в TIMELINE
- Update METRICS table
- Update ROOT CAUSE EVOLUTION если понимание изменилось
- Update CURRENT STATUS и Next Steps

### 3️⃣ Thread НЕ найден - New Issue

**Создай новый thread:**
```bash
1. Copy THREAD-TEMPLATE.md
2. Rename to THREAD-XXX_short-description.md
3. Заполни все секции
4. Update README.md table (добавь новую строку)
```

---

## Активные Threads

| ID | Проблема | Status | Severity | Last Updated | Next Action |
|----|----------|--------|----------|--------------|-------------|
| [THREAD-001](THREAD-001_invalid-device-reconnection.md) | Invalid Device After Reconnect | 🔴 ACTIVE | CRITICAL | 2025-10-21 | Test proactive monitoring fix (Attempt #2) |

### Статусы:
- 🔴 **ACTIVE** - проблема активна, требует fix
- 🟡 **IN PROGRESS** - fix в разработке/тестировании
- 🟢 **RESOLVED** - проблема решена, мониторим
- ⚫ **CLOSED** - проблема полностью закрыта, confirmed fixed

---

## Best Practices

### ✅ DO:
- Читай thread ПЕРЕД анализом новых логов
- Обновляй thread ПОСЛЕ каждого attempt (даже failed)
- Записывай METRICS (числа, проценты, частоту ошибок)
- Цитируй логи как evidence
- Обновляй ROOT CAUSE когда понимание меняется

### ❌ DON'T:
- Не пропускай чтение thread ("я помню что делал")
- Не забывай update METRICS после получения новых логов
- Не удаляй failed attempts (они важны для learning!)
- Не создавай дубликаты threads для одной проблемы

---

## Примеры использования

### Пример 1: Regression после fix

```markdown
Получили логи от Joshua → Читаем README.md →
Видим THREAD-001 (Invalid Device) →
Читаем timeline: Attempt #1 failed →
Ожидали: [DISCONNECT] events в логах →
Реально: NO [DISCONNECT] events →
Вывод: Fix НЕ сработал →
Update THREAD-001:
  - Add Attempt #1 result: FAILED
  - Update metrics: 0% improvement
  - Update root cause: iOS doesn't generate events for physical power off
  - Add Attempt #2 plan: Proactive monitoring
```

### Пример 2: Новая проблема

```markdown
Получили логи → Читаем README.md →
Симптомы НЕ совпадают с existing threads →
Создаем THREAD-002_new-problem.md →
Заполняем template →
Add в README.md table →
Начинаем анализ с контекстом
```

---

## FAQ

**Q: Нужно ли создавать thread для мелких bugs?**
A: НЕТ. Thread только для:
- Повторяющихся проблем
- Critical/High severity
- Проблем которые требуют нескольких attempts

**Q: Когда можно закрыть thread (⚫ CLOSED)?**
A: Когда:
- SUCCESS CRITERIA полностью выполнены
- Клиент confirmed fix работает
- Нет рецидивов в течение 2+ недель
- Metrics стабильны

**Q: Можно ли reopenить closed thread?**
A: ДА! Если проблема вернулась:
- Change status: ⚫ CLOSED → 🔴 ACTIVE
- Add новую запись в TIMELINE
- Explain почему reopened

---

**Created:** 2025-10-21
**Last Updated:** 2025-10-21
